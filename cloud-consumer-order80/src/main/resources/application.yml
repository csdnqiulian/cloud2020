server:
  port: 81

spring:
  application:
    name: cloud-order-service

eureka:
  instance:
    instance-id: orderservice81
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:7001/eureka